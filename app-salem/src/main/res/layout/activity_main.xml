<?xml version="1.0" encoding="utf-8"?>
<!--
  WickedSalemWitchCityTour v1.0
  Copyright (c) 2026 Dean Maurice Ellis. All rights reserved.
  Module: activity_main.xml

  This source code is proprietary and confidential.
  Unauthorized copying, modification, or distribution is
  strictly prohibited.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- AppBar with Toolbar (Windows-style menu bar) -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="0dp"
            android:background="?attr/colorPrimary"
            android:elevation="4dp"
            app:contentInsetStart="0dp"
            app:contentInsetEnd="0dp"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            app:title="Katrina's Mystic Visitors Guide">

            <include layout="@layout/toolbar_two_row" />

        </androidx.appcompat.widget.Toolbar>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Map fills remaining space below toolbar -->
    <org.osmdroid.views.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- S146 #27: Persistent narration hero banner — anchored below the
         AppBarLayout, overlays the top of the map. Shows currently-narrating
         (or last-narrated) POI with thumbnail + title + play-state icon. -->
    <LinearLayout
        android:id="@+id/narrationHeroBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#E61E1E2A"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:elevation="6dp"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone"
        app:layout_anchor="@id/appBarLayout"
        app:layout_anchorGravity="bottom|fill_horizontal">

        <ImageView
            android:id="@+id/heroThumbnail"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:scaleType="centerCrop"
            android:src="@drawable/welcome_katrina_avatar"
            android:background="#2A2A3C"
            android:contentDescription="POI image"
            android:layout_marginEnd="10dp" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/heroTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#F5F0E8"
                android:textSize="14sp"
                android:textStyle="bold"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="" />

            <TextView
                android:id="@+id/heroSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#B8AFA0"
                android:textSize="10sp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="" />

        </LinearLayout>

        <ImageView
            android:id="@+id/heroPlayState"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_hero_idle"
            android:contentDescription="Narration state"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <!-- Custom zoom slider (right edge, vertically centered) -->
    <LinearLayout
        android:id="@+id/zoomSliderContainer"
        android:layout_width="48dp"
        android:layout_height="280dp"
        android:layout_gravity="end|center_vertical"
        android:layout_marginEnd="4dp"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:elevation="8dp">

        <!-- Zoom IN button (+) -->
        <TextView
            android:id="@+id/btnZoomIn"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/zoom_button_bg"
            android:text="+"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true" />

        <!-- Track area with bubble -->
        <FrameLayout
            android:id="@+id/zoomTrackArea"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp">

            <!-- Vertical track line -->
            <View
                android:id="@+id/zoomTrackLine"
                android:layout_width="4dp"
                android:layout_height="match_parent"
                android:layout_gravity="center_horizontal"
                android:background="@drawable/zoom_slider_track" />

            <!-- Zoom level bubble (positioned programmatically) -->
            <TextView
                android:id="@+id/zoomBubble"
                android:layout_width="wrap_content"
                android:layout_height="24dp"
                android:minWidth="36dp"
                android:layout_gravity="center_horizontal"
                android:background="@drawable/zoom_bubble_bg"
                android:text="14"
                android:textColor="@android:color/white"
                android:textSize="11sp"
                android:textStyle="bold"
                android:gravity="center"
                android:elevation="2dp" />

        </FrameLayout>

        <!-- Zoom OUT button (-) -->
        <TextView
            android:id="@+id/btnZoomOut"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/zoom_button_bg"
            android:text="−"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true" />

    </LinearLayout>

    <!-- Bottom-left debug/tool button stack -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="168dp"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:elevation="16dp">

        <!-- GPS Journey path visibility toggle (Phase 9T+, S109/S110) -->
        <TextView
            android:id="@+id/btnGpsToggle"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/zoom_toggle_bg"
            android:text="GPS"
            android:textColor="@android:color/white"
            android:textSize="10sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Toggle GPS journey path visibility" />

        <!-- Show All POIs toggle -->
        <TextView
            android:id="@+id/btnShowAllPois"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/zoom_toggle_bg"
            android:text="POI"
            android:textColor="@android:color/white"
            android:textSize="10sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true" />

        <!-- Walk simulator toggle -->
        <TextView
            android:id="@+id/btnWalkSim"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/zoom_toggle_bg"
            android:text="Walk"
            android:textColor="@android:color/white"
            android:textSize="9sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true" />

        <!-- S115: Heading-up / North-up map rotation toggle -->
        <TextView
            android:id="@+id/btnHeadingUp"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/zoom_toggle_bg"
            android:text="N\u2191"
            android:textColor="@android:color/white"
            android:textSize="11sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Toggle heading-up map rotation" />

        <!-- Magnify toggle -->
        <FrameLayout
            android:id="@+id/zoomToggleBtn"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/zoom_toggle_bg"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="center"
                android:layout_marginBottom="4dp"
                android:src="@drawable/ic_zoom_toggle"
                android:contentDescription="Zoom toggle" />

            <TextView
                android:id="@+id/zoomToggleLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="4dp"
                android:text="x1"
                android:textColor="@android:color/white"
                android:textSize="10sp"
                android:textStyle="bold" />

        </FrameLayout>

    </LinearLayout>

    <!-- FAB speed dial container (bottom-right) -->
    <LinearLayout
        android:id="@+id/fabMenu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="72dp"
        android:orientation="vertical"
        android:gravity="end"
        android:visibility="gone" />

    <!-- Main FAB trigger -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:layout_marginBottom="100dp"
        android:src="@android:drawable/ic_menu_add"
        app:tint="@android:color/white" />

    <!-- Proximity dock: nearby POI icons at bottom of screen (Phase 9T) -->
    <include layout="@layout/proximity_dock" />

    <!-- Narration bottom sheet: slides up on geofence trigger or dock tap (Phase 9T) -->
    <include layout="@layout/narration_bottom_sheet" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
